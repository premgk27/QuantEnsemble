# Lambda-specific requirements (no jupyter, no matplotlib, no seaborn)
numpy>=1.26
pandas>=2.2
scikit-learn>=1.5
requests>=2.32.5
# boto3 is pre-installed in the Lambda Python runtime — no need to include it
